{
  "tx_id": "hash of all other data in transaction",
  "locktime": "unix time in seconds until outputs are valid",
  "inputs": [
    {
      "previous_output": ["block index", "transaction index", "output index"],
      "signature_script": "Conditions that satisfy output's pubkey script [PUBKEY]"
    }
  ],
  "outputs": [
    {
      "value": "value of output in 100ths of a coin",
      "pk_script": "Conditions that must be met to redeem this output stored in a list [PUBKEY]"
    }
  ],
  "user_data" : {"pk" :  "user public key",
                 "signature" :  "transaction signature"}
}